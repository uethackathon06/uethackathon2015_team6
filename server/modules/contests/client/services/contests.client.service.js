'use strict';

angular.module('contests').factory('ContestService',[
  function () {
  	//the object is shuffled randomly and the key is generated
  	var createShuffle = function(obj){
  		return shuffleKey;
  	}

  	//given an obj and key, restore that obj
  	var restoreShuffle = function(obj, key){

  	}
    return {};
  }
]);
